export default function AuthRoutePage() {
  return (
    <section className="rounded-3xl border border-border bg-card p-10 shadow-sm">
      <h1 className="text-2xl font-semibold text-foreground">Auth route shell</h1>
      <p className="mt-4 text-sm text-muted-foreground">
        Authentication routes will live here as the architecture expands.
      </p>
    </section>
  );
}
